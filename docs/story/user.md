# User Stories домена "User"

## US-1: Регистрация пользователя

### Описание

> **Как** новый пользователь  
> **Я хочу** иметь возможность зарегистрироваться в системе  
> **Чтобы** получить доступ к функциям и сервисам приложения

### Критерии приемки

**Функциональные требования:**
* Форма регистрации должна содержать поля:
  * Email (обязательное, уникальное)
  * First Name (обязательное)
  * Last Name (обязательное)
  * Password (обязательное)
* Пароль должен соответствовать требованиям безопасности:
  * Минимум 8 символов
  * Содержит как минимум одну заглавную букву
  * Содержит как минимум одну строчную букву
  * Содержит как минимум одну цифру
  * Содержит как минимум один специальный символ
* После успешной регистрации пользователь перенаправляется на главную страницу
* Пользователь автоматически аутентифицируется после успешной регистрации
* Отображается сообщение о необходимости подтверждения аккаунта администратором
* Отображаются понятные сообщения об ошибках валидации

**Нефункциональные требования:**
* Пароль хранится в зашифрованном виде
* Email должен быть валидным
* Форма должна быть защищена от CSRF атак
* Ответ системы должен приходить в течение 2 секунд

### Исходное состояние

* Пользователь не аутентифицирован
* Пользователь находится на главной странице сайта

### Сценарий использования (Happy Path)

**Шаг 1: Открытие формы регистрации**
* Пользователь заходит на сайт
* В шапке видна кнопка "Зарегистрироваться"
* При нажатии на кнопку открывается форма регистрации

**Шаг 2: Заполнение формы**
Пользователь заполняет поля:
* Email: корректный email адрес
* First Name: имя
* Last Name: фамилия
* Password: пароль, соответствующий требованиям безопасности

**Шаг 3: Отправка формы**
* Пользователь нажимает кнопку "Зарегистрироваться"
* Форма проходит валидацию
* Данные отправляются на сервер

**Шаг 4: Обработка на сервере**
* Система валидирует данные
* Проверяет уникальность email
* Создает учетную запись пользователя
* Генерирует токен аутентификации

**Шаг 5: Успешное завершение**
* Пользователь перенаправляется на главную страницу
* Токен сохраняется для последующих запросов
* В шапке отображается имя пользователя и кнопка "Выйти"
* На главной странице отображается информационное сообщение:
  * **Заголовок:** "Регистрация прошла успешно"
  * **Текст:** "Ваш аккаунт успешно создан и вы вошли в систему. Для получения доступа к функциям приложения необходимо назначение роли. Пожалуйста, обратитесь к системному администратору для подтверждения вашего аккаунта."
* Пользователь аутентифицирован, но имеет ограниченный доступ до назначения роли администратором

### Альтернативные сценарии

#### A1: Email уже существует
**Когда:** Пользователь вводит email, который уже зарегистрирован в системе  
**Тогда:**
* Поле email подсвечивается красным
* Под полем отображается: "Пользователь с таким email уже существует"
* Форма не отправляется

#### A2: Невалидный пароль
**Когда:** Пользователь вводит пароль, не соответствующий требованиям безопасности  
**Тогда:**
* Поле password подсвечивается красным
* Отображается список невыполненных требований:
  * ❌ Минимум 8 символов
  * ❌ Заглавная буква
  * ❌ Строчная буква
  * ❌ Цифра
  * ❌ Специальный символ
* Форма не отправляется

#### A3: Невалидный email
**Когда:** Пользователь вводит некорректный email адрес  
**Тогда:**
* Поле email подсвечивается красным
* Под полем отображается: "Введите корректный email адрес"
* Форма не отправляется

#### A4: Пустые обязательные поля
**Когда:** Пользователь оставляет обязательные поля незаполненными  
**Тогда:**
* Незаполненные поля подсвечиваются красным
* Под каждым полем отображается: "Это поле обязательно для заполнения"
* Форма не отправляется

#### A5: Ошибка системы
**Когда:** Происходит непредвиденная ошибка
**Тогда:**
* Отображается общее сообщение об ошибке: "Произошла ошибка. Попробуйте позже"
* Данные формы сохраняются
* Пользователь может повторить попытку

### Дополнительная информация

**Статус пользователя после регистрации:**
* Пользователь аутентифицирован (имеет действующий токен)
* Роль не назначена (требуется действие администратора)
* Доступ к основным функциям системы ограничен до назначения роли
* Пользователь может выйти из системы в любой момент

---

## US-2: Выход из системы

### Описание

> **Как** аутентифицированный пользователь  
> **Я хочу** иметь возможность выйти из системы  
> **Чтобы** завершить свою сессию и защитить аккаунт

### Критерии приемки

* В шапке присутствует кнопка "Выйти" для аутентифицированных пользователей
* При нажатии на кнопку пользователь выходит из системы
* Токен аутентификации удаляется
* Пользователь перенаправляется на главную страницу
* Статус меняется на "не аутентифицирован"
* В шапке отображаются кнопки "Войти" и "Зарегистрироваться"

### Исходное состояние

* Пользователь аутентифицирован
* Токен аутентификации активен
* В шапке отображается кнопка "Выйти"

### Сценарий использования

**Шаг 1:** Пользователь нажимает кнопку "Выйти" в шапке  
**Шаг 2:** Токен аутентификации удаляется  
**Шаг 3:** Состояние приложения обновляется  
**Шаг 4:** Пользователь перенаправляется на главную страницу  
**Шаг 5:** В шапке отображаются кнопки для неаутентифицированных пользователей

### Альтернативные сценарии

#### A1: Токен уже недействителен
**Когда:** Токен аутентификации истек или был удален  
**Тогда:**
* Система корректно обрабатывает ситуацию
* Пользователь перенаправляется на главную страницу
* Отображается состояние неаутентифицированного пользователя

---

## US-3: Вход в систему

### Описание

> **Как** зарегистрированный пользователь
> **Я хочу** иметь возможность войти в систему
> **Чтобы** получить доступ к своему аккаунту и функциям приложения

### Критерии приемки

**Функциональные требования:**
* Форма входа должна содержать поля:
  * Email (обязательное)
  * Password (обязательное)
* После успешного входа пользователь перенаправляется на главную страницу
* Токен аутентификации сохраняется в cookie для последующих запросов
* В шапке отображается имя пользователя и кнопка "Выйти"
* Отображаются понятные сообщения об ошибках аутентификации

**Нефункциональные требования:**
* Форма должна быть защищена от CSRF атак
* Неудачные попытки входа логируются с IP адресом
* После 5 неудачных попыток с одного IP адреса доступ блокируется на 15 минут
* Ответ системы должен приходить в течение 2 секунд
* Токен имеет срок действия 30 дней и сохраняется в защищенном cookie

### Исходное состояние

* Пользователь зарегистрирован в системе
* Пользователь не аутентифицирован
* Пользователь находится на главной странице сайта

### Сценарий использования (Happy Path)

**Шаг 1: Открытие формы входа**
* Пользователь заходит на сайт
* В шапке видна кнопка "Войти"
* При нажатии на кнопку открывается форма входа

**Шаг 2: Заполнение формы**
Пользователь заполняет поля:
* Email: зарегистрированный email адрес
* Password: пароль учетной записи

**Шаг 3: Отправка формы**
* Пользователь нажимает кнопку "Войти"
* Форма проходит валидацию
* Данные отправляются на сервер

**Шаг 4: Обработка на сервере**
* Система валидирует данные
* Проверяет существование пользователя с указанным email
* Проверяет соответствие пароля
* Проверяет, не заблокирован ли аккаунт
* Генерирует токен аутентификации
* Записывает информацию о входе в лог

**Шаг 5: Успешное завершение**
* Пользователь перенаправляется на главную страницу
* Токен сохраняется в защищенном cookie (HttpOnly, Secure) со сроком действия 30 дней
* В шапке отображается имя пользователя и кнопка "Выйти"
* Пользователь получает доступ к функциям согласно своей роли

### Альтернативные сценарии

#### A1: Неверный email или пароль
**Когда:** Пользователь вводит неправильный email или пароль
**Тогда:**
* Форма подсвечивается красным
* Отображается сообщение: "Неверный email или пароль"
* Счетчик неудачных попыток для данного IP адреса увеличивается
* Форма остается доступной для повторной попытки
* Данные не отправляются

#### A2: Пустые поля
**Когда:** Пользователь оставляет обязательные поля незаполненными
**Тогда:**
* Незаполненные поля подсвечиваются красным
* Под каждым полем отображается: "Это поле обязательно для заполнения"
* Форма не отправляется

#### A3: IP адрес заблокирован
**Когда:** С одного IP адреса было сделано 5 и более неудачных попыток входа
**Тогда:**
* Отображается сообщение: "Слишком много неудачных попыток входа. Доступ временно заблокирован. Попробуйте через 15 минут"
* Форма недоступна для отправки
* Таймер показывает оставшееся время блокировки
* В логе записывается попытка входа с заблокированного IP
* Блокировка применяется только к данному IP адресу, не к аккаунту пользователя

#### A4: Пользователь не найден
**Когда:** Указан email незарегистрированного пользователя
**Тогда:**
* Отображается то же сообщение, что и при неверном пароле: "Неверный email или пароль"
* Это сделано в целях безопасности, чтобы не раскрывать информацию о существовании email в системе
* Форма остается доступной для повторной попытки

#### A5: Ошибка системы
**Когда:** Происходит непредвиденная ошибка на сервере
**Тогда:**
* Отображается общее сообщение об ошибке: "Произошла ошибка. Попробуйте позже"
* Данные формы сохраняются
* Пользователь может повторить попытку
* Ошибка логируется для анализа

#### A6: Токен истек
**Когда:** Пользователь уже аутентифицирован, но токен истек
**Тогда:**
* При попытке выполнить защищенное действие система автоматически перенаправляет на форму входа
* Отображается сообщение: "Ваша сессия истекла. Пожалуйста, войдите снова"
* После успешного входа пользователь возвращается на страницу, с которой был перенаправлен

### Дополнительная информация

**Безопасность:**
* Пароли передаются по защищенному соединению (HTTPS)
* Токены подписываются для предотвращения подделки
* Неудачные попытки входа логируются с IP адресом и временной меткой
* После 5 неудачных попыток с одного IP адреса доступ блокируется на 15 минут
* Блокировка применяется к IP адресу, а не к аккаунту пользователя
* Блокировка снимается автоматически через 15 минут
* Пользователь может попытаться войти с другого IP адреса, если блокирован текущий

**Хранение токена:**
* Токен действителен 30 дней
* Токен сохраняется в защищенном cookie с флагами HttpOnly и Secure
* Автоматическое продление токена при активности пользователя

**Восстановление доступа:**
* Если пользователь забыл пароль, доступна ссылка "Забыли пароль?" (реализуется отдельной US)
* Если IP адрес заблокирован, пользователь может подождать 15 минут или войти с другого устройства/сети

**Логирование:**
* Успешные входы записываются с IP адресом и временной меткой
* Неудачные попытки логируются с причиной (неверный пароль, несуществующий email, IP заблокирован)
* Счетчик неудачных попыток ведется отдельно для каждого IP адреса
* Информация доступна администраторам для аудита

---

## US-4: Назначение роли администратора через CLI

### Описание

> **Как** системный администратор  
> **Я хочу** иметь возможность назначить роль администратора пользователю через CLI скрипт  
> **Чтобы** предоставить доступ к административным функциям системы

### Критерии приемки

**Функциональные требования:**
* CLI скрипт доступен на сервере для системного администратора
* Скрипт принимает email пользователя как обязательный параметр
* Скрипт проверяет существование пользователя с указанным email
* Скрипт назначает роль "администратор" найденному пользователю
* Скрипт выводит информацию о результате операции
* После назначения роли пользователь получает доступ ко всем функциям системы
* Операция логируется для аудита

**Нефункциональные требования:**
* Скрипт требует SSH доступ к серверу
* Операция должна выполняться атомарно (либо успех, либо откат)
* Изменения применяются немедленно
* Время выполнения не превышает 5 секунд

### Исходное состояние

* Системный администратор имеет SSH доступ к серверу
* В системе зарегистрирован пользователь без роли
* Пользователь аутентифицирован, но не имеет доступа к функциям

### Сценарий использования (Happy Path)

**Шаг 1: Подключение к серверу**
* Системный администратор подключается к серверу по SSH
* Переходит в директорию с приложением

**Шаг 2: Запуск CLI скрипта**
* Администратор выполняет команду:
  ```bash
  uv run python -m app.cli assign-admin-role --email user@example.com
  ```

**Шаг 3: Обработка запроса**
* Скрипт подключается к базе данных
* Выполняет поиск пользователя по email
* Находит пользователя
* Назначает роль "администратор"
* Сохраняет изменения в базе данных

**Шаг 4: Вывод результата**
* Скрипт выводит сообщение:
  ```
  ✓ Роль администратора успешно назначена пользователю:
    Email: user@example.com
    Имя: Иван Иванов
    ID: uuid
  ```

**Шаг 5: Применение изменений**
* Изменения применяются немедленно
* При следующем входе пользователь получает доступ ко всем функциям
* Если пользователь уже в системе, изменения вступают в силу при обновлении токена

### Альтернативные сценарии

#### A1: Пользователь не найден
**Когда:** Указан email несуществующего пользователя  
**Тогда:**
* Скрипт выводит сообщение:
  ```
  ✗ Ошибка: Пользователь с email user@example.com не найден
  ```
* Операция прерывается
* Изменения не применяются

#### A2: У пользователя уже есть роль администратора
**Когда:** Пользователь уже имеет роль администратора  
**Тогда:**
* Скрипт выводит предупреждение:
  ```
  ⚠ Предупреждение: Пользователь user@example.com уже является администратором
  ```
* Операция завершается успешно без изменений

#### A3: Ошибка подключения к базе данных
**Когда:** Невозможно подключиться к базе данных  
**Тогда:**
* Скрипт выводит сообщение об ошибке:
  ```
  ✗ Ошибка: Не удалось подключиться к базе данных
  Проверьте настройки подключения и доступность сервера БД
  ```
* Операция прерывается

#### A4: Некорректный формат email
**Когда:** Указан невалидный email адрес  
**Тогда:**
* Скрипт выводит сообщение:
  ```
  ✗ Ошибка: Некорректный формат email адреса
  ```
* Операция прерывается до обращения к базе данных

#### A5: Недостаточно прав доступа
**Когда:** У системного администратора недостаточно прав для выполнения операции  
**Тогда:**
* Скрипт выводит сообщение:
  ```
  ✗ Ошибка: Недостаточно прав для выполнения операции
  Убедитесь, что у вас есть доступ к базе данных и права на изменение ролей
  ```
* Операция прерывается

### Дополнительная информация

**Варианты использования скрипта:**
* Назначение роли конкретному пользователю: `--email user@example.com`
* Просмотр справки: `--help`
* Вывод текущей роли пользователя: `--email user@example.com --show-current`

**Логирование:**
* Все операции назначения ролей записываются в журнал аудита
* Логи содержат: timestamp, email администратора, email пользователя, результат операции

**Безопасность:**
* Доступ к скрипту только через SSH
* Требуется корректная конфигурация подключения к БД
* Операции выполняются от имени приложения с ограниченными правами

---

## US-5: Удаление роли администратора через CLI

### Описание

> **Как** системный администратор  
> **Я хочу** иметь возможность удалить роль администратора у пользователя через CLI скрипт  
> **Чтобы** отозвать доступ к административным функциям системы

### Критерии приемки

**Функциональные требования:**
* CLI скрипт доступен на сервере для системного администратора
* Скрипт принимает email пользователя как обязательный параметр
* Скрипт проверяет существование пользователя с указанным email
* Скрипт проверяет наличие роли администратора у пользователя
* Скрипт удаляет роль "администратор" у найденного пользователя
* Скрипт выводит информацию о результате операции
* После удаления роли пользователь теряет доступ к административным функциям
* Операция логируется для аудита

**Нефункциональные требования:**
* Скрипт требует SSH доступ к серверу
* Операция должна выполняться атомарно (либо успех, либо откат)
* Изменения применяются немедленно
* Время выполнения не превышает 5 секунд

### Исходное состояние

* Системный администратор имеет SSH доступ к серверу
* В системе есть пользователь с ролью администратора
* Пользователь может быть аутентифицирован в системе

### Сценарий использования (Happy Path)

**Шаг 1: Подключение к серверу**
* Системный администратор подключается к серверу по SSH
* Переходит в директорию с приложением

**Шаг 2: Запуск CLI скрипта**
* Администратор выполняет команду:
  ```bash
  uv run python -m app.cli revoke-admin-role --email user@example.com
  ```

**Шаг 3: Обработка запроса**
* Скрипт подключается к базе данных
* Выполняет поиск пользователя по email
* Находит пользователя
* Проверяет наличие роли "администратор"
* Удаляет роль "администратор"
* Сохраняет изменения в базе данных

**Шаг 4: Вывод результата**
* Скрипт выводит сообщение:
  ```
  ✓ Роль администратора успешно отозвана у пользователя:
    Email: user@example.com
    Имя: Иван Иванов
    ID: uuid
  ```

**Шаг 5: Применение изменений**
* Изменения применяются немедленно
* Если пользователь в системе, изменения вступают в силу при следующем запросе
* Пользователь теряет доступ к административным функциям
* При попытке доступа к административным функциям отображается сообщение об отсутствии прав

### Альтернативные сценарии

#### A1: Пользователь не найден
**Когда:** Указан email несуществующего пользователя  
**Тогда:**
* Скрипт выводит сообщение:
  ```
  ✗ Ошибка: Пользователь с email user@example.com не найден
  ```
* Операция прерывается
* Изменения не применяются

#### A2: У пользователя нет роли администратора
**Когда:** Пользователь не имеет роли администратора  
**Тогда:**
* Скрипт выводит предупреждение:
  ```
  ⚠ Предупреждение: Пользователь user@example.com не является администратором
  ```
* Операция завершается успешно без изменений

#### A3: Ошибка подключения к базе данных
**Когда:** Невозможно подключиться к базе данных  
**Тогда:**
* Скрипт выводит сообщение об ошибке:
  ```
  ✗ Ошибка: Не удалось подключиться к базе данных
  Проверьте настройки подключения и доступность сервера БД
  ```
* Операция прерывается

#### A4: Некорректный формат email
**Когда:** Указан невалидный email адрес  
**Тогда:**
* Скрипт выводит сообщение:
  ```
  ✗ Ошибка: Некорректный формат email адреса
  ```
* Операция прерывается до обращения к базе данных

#### A5: Недостаточно прав доступа
**Когда:** У системного администратора недостаточно прав для выполнения операции  
**Тогда:**
* Скрипт выводит сообщение:
  ```
  ✗ Ошибка: Недостаточно прав для выполнения операции
  Убедитесь, что у вас есть доступ к базе данных и права на изменение ролей
  ```
* Операция прерывается

#### A6: Попытка отозвать роль у последнего администратора
**Когда:** Попытка удалить роль у единственного администратора в системе  
**Тогда:**
* Скрипт выводит предупреждение:
  ```
  ⚠ Предупреждение: Это единственный администратор в системе
  Отзыв роли может привести к невозможности администрирования системы
  Продолжить? (y/N):
  ```
* Требуется явное подтверждение операции
* При отказе операция отменяется
* При подтверждении операция выполняется

### Дополнительная информация

**Варианты использования скрипта:**
* Удаление роли у конкретного пользователя: `--email user@example.com`
* Просмотр справки: `--help`
* Удаление с подтверждением: `--email user@example.com --force` (пропускает подтверждение для последнего админа)

**Логирование:**
* Все операции отзыва ролей записываются в журнал аудита
* Логи содержат: timestamp, email системного администратора, email пользователя, результат операции

**Безопасность:**
* Доступ к скрипту только через SSH
* Требуется корректная конфигурация подключения к БД
* Операции выполняются от имени приложения с ограниченными правами
* Рекомендуется проверить количество администраторов перед удалением роли

---

## US-6: Управление ролями пользователей через веб-интерфейс

### Описание

> **Как** администратор  
> **Я хочу** иметь возможность просматривать список пользователей и назначать им роли через веб-интерфейс  
> **Чтобы** эффективно управлять доступами в системе

### Критерии приемки

**Функциональные требования:**
* Страница управления пользователями доступна только администраторам
* Отображается список всех зарегистрированных пользователей
* Для каждого пользователя отображается:
  * Имя (First Name + Last Name)
  * Email
  * Текущая роль (или "Роль не назначена")
  * Дата регистрации
* Доступна возможность назначить роль "Учитель" или "Студент"
* Доступна возможность изменить существующую роль
* Изменения применяются немедленно после подтверждения
* Отображается уведомление об успешном назначении/изменении роли
* Присутствует поиск по имени или email
* Присутствует фильтрация по ролям (Все / Без роли / Учитель / Студент / Администратор)
* Поддерживается пагинация для больших списков (по 20 пользователей на страницу)

**Нефункциональные требования:**
* Страница загружается менее чем за 2 секунды
* Интерфейс интуитивно понятен и удобен
* Операции защищены от несанкционированного доступа
* Все действия логируются

### Исходное состояние

* Пользователь аутентифицирован с ролью "Администратор"
* В системе есть зарегистрированные пользователи
* Некоторые пользователи могут не иметь назначенной роли

### Сценарий использования (Happy Path)

**Шаг 1: Переход на страницу управления пользователями**
* Администратор входит в систему
* В навигационном меню выбирает пункт "Управление пользователями"
* Открывается страница со списком пользователей

**Шаг 2: Просмотр списка пользователей**
* Отображается таблица с пользователями
* Столбцы таблицы:
  * Имя
  * Email
  * Роль
  * Дата регистрации
  * Действия

**Шаг 3: Назначение роли пользователю**
* Администратор находит пользователя без роли
* В столбце "Действия" нажимает кнопку "Назначить роль"
* Открывается модальное окно с выбором роли:
  * 🎓 Учитель
  * 📚 Студент
* Администратор выбирает роль "Учитель"
* Нажимает кнопку "Подтвердить"

**Шаг 4: Подтверждение операции**
* Модальное окно закрывается
* Отображается уведомление: "✓ Роль 'Учитель' успешно назначена пользователю [Имя Фамилия]"
* В таблице обновляется информация о роли пользователя
* Строка пользователя больше не выделена цветом

**Шаг 5: Использование фильтров**
* Администратор может применить фильтр "Без роли" для просмотра только пользователей без назначенной роли
* Может использовать поиск для быстрого нахождения конкретного пользователя

### Альтернативные сценарии

#### A1: Изменение существующей роли
**Когда:** Администратор хочет изменить роль пользователя с "Студент" на "Учитель"  
**Тогда:**
* В столбце "Действия" нажимает кнопку "Изменить роль"
* Открывается модальное окно с текущей ролью
* Выбирает новую роль "Учитель"
* Подтверждает изменение
* Роль успешно обновляется
* Отображается уведомление об успешном изменении

#### A2: Поиск пользователя
**Когда:** Администратор ищет конкретного пользователя
**Тогда:**
* Вводит в поле поиска имя или email
* Список фильтруется в реальном времени
* Отображаются только совпадающие результаты
* Если совпадений нет, отображается сообщение "Пользователи не найдены"

#### A3: Фильтрация по роли
**Когда:** Администратор хочет увидеть только пользователей без роли  
**Тогда:**
* Выбирает фильтр "Без роли" из выпадающего списка
* Список обновляется, показывая только пользователей без назначенной роли
* Счетчик показывает количество найденных пользователей

#### A4: Попытка доступа не-администратором
**Когда:** Пользователь без роли администратора пытается открыть страницу управления  
**Тогда:**
* Отображается страница с ошибкой 403 (Доступ запрещен)
* Сообщение: "У вас недостаточно прав для доступа к этой странице"
* Кнопка "Вернуться на главную"

#### A5: Ошибка при назначении роли
**Когда:** Происходит ошибка при сохранении роли на сервере  
**Тогда:**
* Модальное окно остается открытым
* Отображается сообщение об ошибке: "Не удалось назначить роль. Попробуйте позже"
* Администратор может повторить попытку
* Данные в таблице не изменяются

#### A6: Пустой список пользователей
**Когда:** В системе нет зарегистрированных пользователей (кроме администратора)  
**Тогда:**
* Отображается сообщение: "В системе пока нет других пользователей"
* Показывается информационный блок о том, что пользователи появятся после регистрации

### Дополнительная информация

**Структура модального окна назначения роли:**
* Заголовок: "Назначить роль пользователю [Имя Фамилия]"
* Информация о пользователе:
  * Имя: [First Name Last Name]
  * Email: [user@example.com]
* Выбор роли:
  * 🎓 Учитель - доступ к созданию и управлению курсами
  * 📚 Студент - доступ к просмотру курсов и выполнению заданий
* Кнопки: "Отмена" и "Подтвердить"

**Визуальные индикаторы:**
* Пользователи без роли: фон строки желтый (#FFF3CD)
* Учитель: иконка 🎓 перед названием роли
* Студент: иконка 📚 перед названием роли
* Администратор: иконка 👑 перед названием роли (роль нельзя изменить через веб-интерфейс)

**Пагинация:**
* По 20 пользователей на страницу
* Элементы управления: "Предыдущая" / номера страниц / "Следующая"
* Отображается общее количество пользователей

**Сортировка:**
* По умолчанию: сначала пользователи без роли, затем по дате регистрации (новые первые)
* Доступна сортировка по всем столбцам таблицы

**Логирование:**
* Все назначения и изменения ролей записываются в журнал аудита
* Логи содержат: timestamp, email администратора, email пользователя, старая роль, новая роль

**Права доступа:**
* Страница доступна только пользователям с ролью "Администратор"
* Администраторы не могут изменить роль другим администраторам через веб-интерфейс
* Для этого требуется использование CLI скриптов
